<#include "CopyRight.ftl">
package ${project.packageName}.extend.service;

import ${project.packageName}.base.model.${entity.name};
import ${project.packageName}.base.service.${entity.name}Service;

public interface ${entity.name}ExtendService extends ${entity.name}Service{

}
